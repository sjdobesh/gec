# gec
Basic rendering engine built in C with as little third party libraries as possible. Uses SDL2.

Compile with : 
g++ -o objParse objParse.c `sdl2-config --cflags --libs` -lSDL2 -lSDL2_mixer -lSDL2_image -lSDL2_ttf

